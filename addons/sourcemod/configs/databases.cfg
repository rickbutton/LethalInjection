"Databases"
{
	"driver_default"		"mysql"
	
	// When specifying "host", you may use an IP address, a hostname, or a socket file path
	
	"default"
	{
		"driver"			"default"
		"host"				"localhost" // <-- your server ip here
		"database"			"sourcemod"
		"user"				"root" // <-- user
		"pass"				"lethal" // <-- password
		"port"				"3306"
	}
	
	"storage-local"
	{
		"driver"			"sqlite"
		"database"			"sourcemod-local"
	}

	"clientprefs"
	{
		"driver"			"sqlite"
		"host"				"" // <-- your server ip here
		"database"			"clientprefs-sqlite"
		"user"				"" // <-- user
		"pass"				"" // <-- password
	}
	"level50_db"
	{
		"driver"			"mysql"
		"host"				"localhost" // <-- your server ip here
		"database"			"sourcemod"
		"user"				"root" // <-- user
		"pass"				"lethal" // <-- password
	}
}
